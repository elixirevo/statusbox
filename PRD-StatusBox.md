# Status Box 기능 명세서

작성일: 2026-05-24

## 1. Summary

Status Box는 macOS 메뉴 막대의 상태 아이콘을 사용자가 정한 범위 단위로 숨기고, 필요할 때 다시 보여주는 메뉴 바 유틸리티 앱이다.
숨겨진 아이콘은 메뉴 막대 위에 다시 펼쳐서 보여주거나, Status Box 아이콘 아래에 뜨는 박스 UI 안에서 보여줄 수 있다.

## 2. Contacts

| 이름 | 역할 | 비고 |
| --- | --- | --- |
| 제품 담당자 | 기능 범위와 우선순위 결정 | 미정 |
| macOS 개발자 | 메뉴 바, 권한, 멀티 디스플레이 구현 | 미정 |
| 디자이너 | 메뉴 바 아이콘, 박스 UI, 설정 화면 설계 | 미정 |
| QA 담당자 | macOS 버전과 멀티 모니터 테스트 | 미정 |

## 3. Background

macOS를 오래 사용하면 메뉴 막대 오른쪽에 많은 상태 아이콘이 쌓인다.
사용자는 자주 쓰지 않는 아이콘을 평소에는 숨기고, 필요할 때만 빠르게 열고 싶어 한다.

Status Box는 메뉴 막대 공간을 정리하면서도 기존 상태 아이콘의 접근성을 유지하는 것을 목표로 한다.
특히 멀티 모니터 환경에서 화면마다 아이콘을 보여주는 방식을 다르게 설정할 수 있게 한다.

### 주요 문제

- 메뉴 막대 상태 아이콘이 많아지면 화면이 복잡해진다.
- 작은 화면이나 노치가 있는 Mac에서는 메뉴 막대 공간이 더 부족하다.
- 숨긴 아이콘을 다시 찾는 동작이 느리면 사용성이 떨어진다.
- 외부 모니터와 내장 디스플레이를 함께 쓸 때 같은 정책이 항상 맞지 않는다.

## 4. Objective

### 목표

사용자가 메뉴 막대 상태 아이콘을 원하는 범위만큼 숨기고, 클릭 한 번으로 다시 볼 수 있게 한다.
또한 숨긴 아이콘을 메뉴 막대 방식 또는 앱 자체 박스 UI 방식으로 화면별로 선택할 수 있게 한다.

### 성공 기준

- 사용자가 최초 실행 후 1분 안에 숨김 범위를 설정할 수 있다.
- Status Box 아이콘 클릭 후 숨긴 아이콘이 300ms 이내에 표시된다.
- 사용자가 메뉴 막대 표시 방식과 박스 UI 표시 방식을 쉽게 전환할 수 있다.
- 멀티 모니터 환경에서 각 모니터의 표시 방식이 저장되고 재실행 후 유지된다.
- 숨김 후 자동 재숨김 시간이 설정값대로 동작한다.

## 5. Market Segment(s)

### 주요 사용자

- 메뉴 막대에 상태 아이콘이 많은 Mac 사용자
- 노트북 화면과 외부 모니터를 함께 쓰는 사용자
- 메뉴 막대를 깔끔하게 유지하고 싶은 생산성 도구 사용자
- 개발자, 디자이너, 파워 유저처럼 백그라운드 앱을 많이 쓰는 사용자

### 사용자 니즈

- 평소에는 불필요한 아이콘을 숨기고 싶다.
- 필요할 때 숨긴 아이콘을 빠르게 보고 클릭하고 싶다.
- 화면마다 다른 방식으로 아이콘을 표시하고 싶다.
- 설정을 자주 바꾸지 않아도 자동으로 원하는 상태가 유지되면 좋겠다.

## 6. Value Proposition(s)

### 핵심 가치

- 메뉴 막대가 깔끔해진다.
- 숨긴 아이콘을 잃어버리지 않고 빠르게 다시 볼 수 있다.
- 메뉴 막대 방식과 박스 UI 방식을 선택할 수 있다.
- 멀티 모니터 환경에 맞춘 세밀한 설정이 가능하다.

### 해결하는 불편

- 메뉴 막대 공간 부족
- 상태 아이콘 과밀
- 숨김 앱을 다시 여는 번거로움
- 모든 모니터에 같은 표시 방식만 적용되는 불편

## 7. Solution

### 7.1 핵심 개념

#### Status Box 아이콘

메뉴 막대에 항상 보이는 앱의 기본 아이콘이다.
왼쪽 클릭 시 숨긴 아이콘을 보여주거나 다시 숨긴다.
오른쪽 클릭 시 컨텍스트 메뉴를 연다.

#### 범위 지정 아이콘

앱 실행 또는 설정 모드 진입 시 메뉴 막대에 두 개의 조절 아이콘을 표시한다.
사용자는 두 아이콘의 위치를 기준으로 숨길 상태 아이콘 범위를 정한다.

예상 동작:

- 왼쪽 경계 아이콘과 오른쪽 경계 아이콘을 메뉴 막대에 표시한다.
- 두 경계 사이에 있는 상태 아이콘을 숨김 대상으로 본다.
- 경계 아이콘은 드래그 또는 설정 모드에서 위치를 바꿀 수 있다.
- 설정이 끝나면 경계 아이콘은 숨기거나 최소 표시 상태로 전환할 수 있다.

#### 숨김 대상 아이콘

사용자가 설정한 범위 안에 들어간 타사 앱 상태 아이콘이다.
Status Box는 이 아이콘들을 평소에는 보이지 않게 처리하고, 사용자가 요청하면 표시한다.

### 7.2 주요 사용자 흐름

#### 최초 실행 흐름

1. 사용자가 Status Box를 실행한다.
2. 앱은 메뉴 막대에 Status Box 아이콘과 범위 지정 아이콘 두 개를 표시한다.
3. 앱은 필요한 macOS 권한을 안내한다.
4. 사용자는 두 범위 지정 아이콘을 움직여 숨길 범위를 정한다.
5. 사용자는 표시 방식을 선택한다.
6. 앱은 설정을 저장하고 숨김 동작을 시작한다.

#### 숨긴 아이콘 보기 흐름

1. 사용자가 Status Box 아이콘을 클릭한다.
2. 현재 모니터의 설정을 확인한다.
3. 표시 방식이 메뉴 막대이면 숨긴 아이콘을 메뉴 막대에 다시 표시한다.
4. 표시 방식이 박스 UI이면 Status Box 아이콘 아래에 박스를 띄운다.
5. 사용자는 표시된 아이콘을 클릭하거나 우클릭한다.
6. 설정된 시간이 지나거나 사용자가 바깥 영역을 클릭하면 아이콘을 다시 숨긴다.

#### 우클릭 메뉴 흐름

1. 사용자가 Status Box 아이콘을 우클릭한다.
2. 컨텍스트 메뉴가 열린다.
3. 사용자는 설정 열기, 숨김/보임 전환, 범위 다시 설정, 앱 종료 등을 선택할 수 있다.

### 7.3 기능 요구사항

#### F1. 상태 아이콘 숨기기

Status Box는 사용자가 지정한 메뉴 막대 범위 안의 상태 아이콘을 숨길 수 있어야 한다.

세부 요구사항:

- 앱 실행 시 두 개의 범위 지정 아이콘을 띄운다.
- 사용자는 범위 지정 아이콘으로 숨길 영역을 정할 수 있다.
- 설정된 범위는 저장되어 앱 재실행 후에도 유지된다.
- Status Box 아이콘 클릭 시 숨긴 아이콘을 보여주거나 다시 숨긴다.
- 숨김 상태와 보임 상태가 명확해야 한다.
- 메뉴 막대가 재배치되거나 디스플레이 구성이 바뀌면 범위를 다시 계산한다.

수용 기준:

- 범위 지정 후 대상 아이콘이 평소에는 보이지 않는다.
- Status Box 아이콘을 클릭하면 대상 아이콘이 표시된다.
- 다시 클릭하면 대상 아이콘이 숨겨진다.

#### F2. 메뉴 막대 표시 방식

숨긴 아이콘을 기존 메뉴 막대 위에 다시 보여주는 방식이다.

세부 요구사항:

- 사용자가 Status Box 아이콘을 클릭하면 숨긴 아이콘이 메뉴 막대에 다시 나타난다.
- 표시된 아이콘은 일반 메뉴 막대 아이콘처럼 클릭할 수 있어야 한다.
- 설정된 자동 재숨김 시간이 지나면 다시 숨긴다.
- 자동 재숨김 시간은 설정에서 변경할 수 있다.

수용 기준:

- 사용자는 숨긴 아이콘을 메뉴 막대에서 다시 클릭할 수 있다.
- 자동 재숨김이 꺼져 있으면 사용자가 직접 닫기 전까지 표시 상태를 유지한다.

#### F3. 박스 UI 표시 방식

숨긴 아이콘을 Status Box 아이콘 아래에 뜨는 박스 안에서 보여주는 방식이다.

세부 요구사항:

- Status Box 아이콘 클릭 시 아이콘 아래에 작은 박스 UI를 표시한다.
- 박스는 현재 활성 모니터의 메뉴 막대 아래에 떠야 한다.
- 박스 안에는 숨긴 아이콘이 메뉴 막대와 비슷한 가로 배열로 표시된다.
- 아이콘 수가 많으면 줄바꿈 또는 스크롤을 지원한다.
- 박스 바깥을 클릭하면 박스를 닫는다.
- 박스 안의 아이콘은 클릭, 우클릭, 길게 누르기 등 가능한 상호작용을 지원한다.
- 박스는 화면 밖으로 나가지 않도록 위치를 보정한다.

수용 기준:

- Status Box 아이콘 클릭 시 박스가 올바른 위치에 나타난다.
- 숨긴 아이콘이 박스 안에 표시된다.
- 사용자가 박스 안 아이콘을 조작할 수 있다.
- 박스가 다른 앱 창보다 위에 표시된다.

#### F4. 모니터별 표시 방식 설정

사용자는 연결된 모니터마다 숨긴 아이콘을 보여주는 방식을 다르게 지정할 수 있어야 한다.

세부 요구사항:

- 설정 화면에 현재 연결된 모니터 목록을 표시한다.
- 각 모니터마다 표시 방식을 선택할 수 있다.
- 선택지는 `메뉴 막대에서 보기`, `박스 UI에서 보기`, `이 모니터에서는 표시하지 않기`를 포함한다.
- 모니터가 추가되거나 제거되면 설정 화면이 갱신된다.
- 저장된 모니터를 다시 연결하면 이전 설정을 복원한다.
- 알 수 없는 새 모니터는 기본 설정을 적용한다.

수용 기준:

- 내장 디스플레이와 외부 모니터에 서로 다른 표시 방식을 저장할 수 있다.
- 앱 재실행 후에도 모니터별 설정이 유지된다.
- 모니터 연결 변경 후 앱이 비정상 종료되지 않는다.

#### F5. 우클릭 컨텍스트 메뉴

Status Box 아이콘 우클릭 시 빠른 조작 메뉴를 제공해야 한다.

메뉴 항목:

- 숨긴 아이콘 보기
- 숨긴 아이콘 다시 숨기기
- 표시 방식 전환
- 범위 다시 설정
- 설정 열기
- 앱 시작 시 자동 실행
- Status Box 종료

수용 기준:

- 우클릭 시 메뉴가 즉시 열린다.
- 설정 열기를 선택하면 설정 UI가 열린다.
- 범위 다시 설정을 선택하면 범위 지정 아이콘이 다시 표시된다.

#### F6. 설정 UI

설정 UI는 숨김 동작과 표시 방식을 관리하는 화면이다.

필수 섹션:

- 일반
- 숨김 범위
- 표시 방식
- 모니터별 설정
- 자동 재숨김
- 권한
- 고급

설정 항목:

- 앱 시작 시 자동 실행
- 클릭 시 동작: 보이기/숨기기 토글, 항상 보이기, 항상 박스 열기
- 숨긴 아이콘 자동 재숨김 시간
- 자동 재숨김 끄기
- 박스 UI 위치 보정
- 박스 UI 아이콘 크기
- 박스 UI 행 수 또는 스크롤 방식
- 모니터별 표시 방식
- 메뉴 막대 변경 감지 후 자동 재정렬
- 범위 다시 설정
- 권한 상태 확인

수용 기준:

- 설정값 변경 후 즉시 동작에 반영된다.
- 앱 재실행 후 설정값이 유지된다.
- 권한이 없을 때 필요한 권한과 해결 방법을 보여준다.

### 7.4 화면 명세

#### 메뉴 막대

- Status Box 기본 아이콘
- 범위 지정 모드에서만 보이는 왼쪽/오른쪽 경계 아이콘
- 보임 상태를 나타내는 아이콘 상태 변화

#### 박스 UI

- Status Box 아이콘 아래에 뜨는 팝오버형 UI
- 상단에는 숨긴 아이콘 영역만 표시한다.
- 아이콘은 메뉴 막대와 비슷한 크기와 간격으로 배치한다.
- 필요 시 하단에 작은 설정 버튼을 둘 수 있다.

#### 설정 창

- macOS 설정 앱과 비슷한 사이드바 또는 탭 구조
- 좌측: 일반, 표시, 모니터, 고급
- 우측: 선택한 설정의 상세 옵션
- 변경 내용은 자동 저장한다.

### 7.5 데이터 모델

#### AppSettings

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| launchAtLogin | Bool | 로그인 시 자동 실행 여부 |
| defaultDisplayMode | Enum | 기본 표시 방식 |
| autoHideEnabled | Bool | 자동 재숨김 사용 여부 |
| autoHideDelaySeconds | Int | 자동 재숨김 시간 |
| boxIconSize | Int | 박스 UI 아이콘 크기 |
| boxMaxRows | Int | 박스 UI 최대 행 수 |

#### HiddenRange

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| displayId | String | 모니터 식별자 |
| leftAnchorPosition | Double | 왼쪽 경계 위치 |
| rightAnchorPosition | Double | 오른쪽 경계 위치 |
| updatedAt | Date | 마지막 수정 시각 |

#### DisplayPolicy

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| displayId | String | 모니터 식별자 |
| displayName | String | 모니터 이름 |
| mode | Enum | 메뉴 막대, 박스 UI, 표시 안 함 |
| lastSeenAt | Date | 마지막 연결 시각 |

### 7.6 권한과 기술 검토

이 앱은 macOS 메뉴 막대와 다른 앱의 상태 아이콘을 다룬다.
따라서 다음 항목은 개발 초기에 반드시 검증해야 한다.

- 접근성 권한이 필요한지
- 화면 기록 권한이 필요한지
- 다른 앱의 상태 아이콘 위치를 안정적으로 감지할 수 있는지
- 숨긴 아이콘을 박스 UI 안에서 실제로 클릭 가능한 형태로 제공할 수 있는지
- 공개 API만으로 구현 가능한 범위와 비공개 API가 필요한 범위
- Mac App Store 배포 가능성
- macOS 버전별 동작 차이

중요 리스크:

- macOS 공개 API는 다른 앱의 메뉴 막대 상태 아이템을 완전히 제어하는 기능을 제한할 수 있다.
- 박스 UI 안에서 타사 상태 아이콘을 원래 아이콘처럼 상호작용하게 만드는 기능은 기술 검증이 필요하다.
- 비공개 API나 화면 좌표 기반 자동화에 의존하면 macOS 업데이트에 취약할 수 있다.

### 7.7 Assumptions

- 사용자는 앱에 필요한 macOS 권한을 허용한다.
- 메뉴 막대 상태 아이콘의 위치를 충분히 안정적으로 추적할 수 있다.
- 멀티 모니터별 메뉴 막대 위치를 식별할 수 있다.
- 숨김 대상 아이콘의 기본 상호작용을 크게 해치지 않고 표시 방식을 바꿀 수 있다.
- 첫 버전은 모든 앱의 특수 메뉴 동작을 완벽히 지원하지 않아도 된다.

## 8. Release

### MVP 범위

- Status Box 메뉴 막대 아이콘
- 숨김 범위 지정 아이콘 두 개
- 범위 기반 상태 아이콘 숨김/보임
- Status Box 아이콘 클릭으로 토글
- 우클릭 컨텍스트 메뉴
- 기본 설정 창
- 자동 재숨김 시간 설정
- 단일 모니터 우선 지원

### v1.1 범위

- 박스 UI 표시 방식
- 박스 UI 아이콘 배열, 줄바꿈, 스크롤
- 박스 바깥 클릭 시 닫기
- 박스 위치 자동 보정

### v1.2 범위

- 모니터별 표시 방식 설정
- 모니터 연결/해제 감지
- 모니터별 숨김 범위 저장
- 새 모니터 기본 정책

### v2.0 후보

- 앱별 숨김 예외 목록
- 아이콘 검색
- 단축키 지원
- 프로필 기능
- iCloud 설정 동기화
- 메뉴 막대 레이아웃 백업과 복원

## 9. 예외 상황

- 숨김 범위 안에 Status Box 아이콘이 들어가면 Status Box 아이콘은 숨기지 않는다.
- 모니터가 제거되면 해당 모니터의 박스 UI를 닫는다.
- 메뉴 막대 아이콘 위치가 바뀌면 다음 표시 시점에 범위를 다시 계산한다.
- 권한이 없으면 숨김 기능을 끄고 권한 안내를 보여준다.
- 전체 화면 앱 사용 중에는 현재 Space와 메뉴 막대 표시 상태에 맞춰 동작한다.
- 노치가 있는 Mac에서는 박스 UI가 노치 영역과 겹치지 않도록 보정한다.

## 10. 비기능 요구사항

- 앱은 백그라운드에서 가볍게 실행되어야 한다.
- 일반 대기 상태에서 CPU 사용량은 매우 낮아야 한다.
- 메뉴 막대 클릭 반응은 즉각적이어야 한다.
- 설정 변경은 앱 재시작 없이 반영되어야 한다.
- 앱이 종료되어도 다른 앱의 메뉴 막대 상태가 손상되지 않아야 한다.
- VoiceOver와 키보드 탐색을 가능한 범위에서 지원해야 한다.
- 사용자의 화면 내용이나 앱 사용 정보를 외부로 전송하지 않는다.

## 11. QA 체크리스트

- macOS 최신 버전과 최소 지원 버전에서 테스트한다.
- Intel Mac과 Apple Silicon Mac에서 테스트한다.
- 내장 디스플레이 단독 환경에서 테스트한다.
- 외부 모니터 1대, 2대 이상 연결 환경에서 테스트한다.
- 노치가 있는 MacBook에서 테스트한다.
- 메뉴 막대 자동 숨김 설정이 켜진 상태에서 테스트한다.
- 전체 화면 앱과 Stage Manager 사용 중 동작을 테스트한다.
- 앱 아이콘 수가 적을 때와 많을 때를 모두 테스트한다.
- 자동 재숨김 시간이 0초, 3초, 10초, 꺼짐일 때를 테스트한다.
- 권한 거부, 권한 회수, 권한 재허용 흐름을 테스트한다.

## 12. 오픈 질문

- 최소 지원 macOS 버전은 무엇인가?
- Mac App Store 배포를 목표로 하는가, 직접 배포를 목표로 하는가?
- 박스 UI 안 아이콘은 원본 상태 아이콘의 실제 상호작용을 100% 지원해야 하는가?
- 범위 지정 아이콘은 항상 표시할 것인가, 설정 모드에서만 표시할 것인가?
- 숨김 대상 앱을 범위 방식 외에 앱별 목록으로도 관리할 것인가?
- 키보드 단축키를 MVP에 포함할 것인가?
